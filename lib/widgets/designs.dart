import 'package:flutter/material.dart';

class DesignProject extends StatelessWidget {
  const DesignProject({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(16),
      child: const Text('Design'),
    );
  }
}
